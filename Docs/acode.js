/**
 *
 *  Arquivo Acode.js
 *   Este arquivo serve como teste
 *   para o plugin Acode React
 */
import React from 'react';
import { View, Text } from 'react-native';

/**
 * o plguin deve criar  import {View,
 * Text, 'StyleSheet'} from 'reac
 * -native'
 */

export default function App() {
	return {
		/* Quando os elementos View Text
    afins forem criados o plugin Deve  referenciar no import se nao existir
    */
	};
}
